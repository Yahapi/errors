{
  "name": "@yahapi/errors",
  "version": "0.2.2",
  "description": "Node.js REST error types and middleware",
  "main": "dist/index.js",
  "author": "Niels Krijger <niels@kryger.nl>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/yahapi/errors.git"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "prepublish": "npm run build"
  }
}
